# Base API used by src/lib/api.ts and general fetches
VITE_API_URL=/api

# Dev backend base (used by proxies or manual builds)
VITE_PROXY_URL=http://localhost:3000

# WebSocket URL for real-time live map updates
VITE_WS_URL=http://localhost:3000

# Toggle ML microservices proxies (:8002, :8005). Set to true only if those servers are running.
VITE_ENABLE_ML=false
